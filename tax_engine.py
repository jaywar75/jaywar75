class State:
    def __init__(self, jurisdiction, category, rate):

        """Initialize attributes of a state tax rate."""

        self.jurisdiction = jurisdiction
        self.category = category
        self.rate = rate
